# 更新日志

每个版本对应一个 `## <版本号>` 段落。GitHub Actions 发版时会把当前版本的段落
自动作为 Release 正文；没写段落就退回自动生成。

---

## v1.2.7

**窗口高度重构 + 运行日志显示精确化 + 状态栏图标放大**

**窗口高度**

- 窗口高度改由隐藏探针按内容**理想高度**统一驱动：展开/折叠日志、规则增减会自动适配，不再手算各种增量
- 新增用户手动拉伸保护：只有真正手动拖过窗口（`windowDidEndLiveResize`）的高度才被保持，内容自己变矮时窗口会跟着缩回，不会被误判成「用户拉高」而拒绝收缩

**运行日志**

- 修复：日志区底部被窗口底边截断（最后一行紧贴窗口下边缘、下内边距丢失）。根因是探针按日志槽位算窗口高度时，漏算真实布局中日志卡片外套的 8pt 内边距（上下共 16pt）——窗口比内容矮 16pt，卡片底边的内边距和圆角边框被窗口裁掉。探针补齐同一段内边距后，窗口与内容严丝合缝
- 优化：日志区展开时**恰好显示完整 4 行**，滚动到底第 5 行不再露出残影。实测 10.5pt 等宽字渲染行高 13.0pt + 行间 1pt 间距 = 每行 14pt，日志槽位定为 4×14 = 56pt；内边距移到滚动区**外面**（不占内容高），内容高 = 行数×14，滚到底正好显示 4 行干净的文字

**界面**

- 状态栏（菜单栏）云朵图标放大 18pt → 22pt，菜单栏更醒目
- 修复：服务器下拉框被页面拉宽时跟着变长、吃满剩余空间——下拉按钮宽度改为只由内容决定，多余空间留给「新增/重命名」等按钮，不再整行拉长

## v1.2.6

**掩码显示修复 + App 图标适配 Mac 风格**

- 修复：服务器地址 / TOKEN 的掩码（圆点）不显示。掩码文本原来画在输入框**下层**，
  被圆角边框的背景盖住，浅色模式下像是一块白色。现在掩码层移到输入框**之上**
  （不拦截点击），颜色用系统语义色，浅色 / 深色模式下都清晰可见、跟随系统外观
- 更新：App 图标适配 Mac 风格

## v1.2.5

**开机自启开关修复 + 隐私优化 + WebDAV 备份管理**

- 修复：关闭开机自启动后，系统设置「登录项 → 允许在后台运行」里的开关仍显示为**开启**。
  根因是 launchd 在 `bootstrap` 时会往 `/var/db/com.apple.xpc.launchd/disabled.<uid>.plist`
  写一条「已启用」记录，而系统设置这个开关就是读它显示的——只删 LaunchAgent 文件、
  卸载任务都不会清掉这条记录。现在关闭时补一条 `launchctl disable` 把记录翻成
  「已禁用」，开关不再卡在开启；重新开启时先 `enable` 再 `bootstrap`，两条路都走通。
  （说明：无 Apple 开发者签名的 App，macOS 只允许注册到「允许在后台运行」，
  「登录时打开」需正式签名，属于系统限制；该类别对菜单栏常驻 App 是正常的）
- 调整：服务器下拉框的箭头颜色**恢复系统默认**（跟随强调色与深浅模式自动适配），
  不再强制成灰色——之前强制改色在白天/夜晚深浅模式下观感不一致

**隐私优化**

- **敏感字段掩码**：服务器地址、TOKEN 输入框收起时用圆点（`*`）遮挡明文，
  点击聚焦才正常显示编辑、失焦回到圆点；监听地址、端口等非敏感信息保持可见，
  防偷窥（typing 截图 / 肩窥）

**WebDAV 备份管理**

- 新增「删除远程 WebDAV 备份」：删除 WebDAV 服务器上的备份文件，本地配置不受影响，
  文件不存在时视为已删除；删除前有二次确认
- 新增「删除 WebDAV 服务器」：移除已保存的 WebDAV 服务器设置，并**同时清除钥匙串里
  的密码**，不再作为备份目标，不可撤销

**界面与默认值**

- 核心配置 / 高级选项字段重新分组：服务器地址、TOKEN、优选域名(IP) 归纳到「核心配置」，
  ECH 域名、DOH 服务器留在「高级选项」，常用项不用再展开
- 新服务器的优选域名(IP) 默认值改为 `cdns.doon.eu.org`，新建即可直接使用，
  不再依赖留空（留空会用 DoH 自动解析）

**构建与内部**

- 修复 Swift 6 并发检查告警：内核管道读取 / 退出回调里安全捕获 `self`，
  避免捕获自引用进入并发域
- 修复 `build.sh` 无 git 仓库时 `git config` 非零退出导致脚本被 `set -e` 中断
- 应用图标换新（新增 1024px 主图 + `refresh-icon.sh` 一键再生成 iconset/icns）

## v1.2.4

**启动「假蓝」修复 + 残留接管自愈**

- 修复：崩溃 / 强杀等异常退出后，系统代理的「接管标记」会残留为真，下次启动菜单栏图标**启动瞬间就变蓝**——但本地端口早已无人监听，这个蓝是假的。现在图标初始不再信任残留标记，**真正接管成功后才变蓝**
- 新增：启动时检测到残留接管（异常退出遗留、端口没人监听），自动**还原系统代理**自愈，避免浏览器流量打到无人监听的旧端口变成黑洞

## v1.2.3

**自动更新双图标优化**

- 启动新实例前，无条件强制结束旧实例并预留时间让系统清理它的菜单栏图标，
  消除「更新完成」弹窗期间新旧两个菜单栏图标短暂并存的现象（此前最终会收敛
  成一个，但点「好」之前能看到两个）

## v1.2.2

**自动更新可靠性修复**

- 修复自动更新后旧版退出、新版却**没有自动重启**（菜单栏图标不再出现）：更新脚本
  改用**直接后台执行二进制**启动新实例。原 `open` 在脚本上下文不可靠——曾出现
  `open` 返回成功却没拉起进程，或新旧 bundle 注册残留导致新实例不启动
- 替换前确保旧进程完全退出：先等 15 秒，仍不退则 `pkill -x` 强制结束，避免新旧
  两个实例同时在菜单栏出现两个图标
- 自动更新替换成功后**自动删除已下载的 DMG**（仅在替换成功时删；失败退回手动
  方式时保留，方便用户手动安装）
- 退出时记录代理是否在运行，下次启动（含自动更新重启）**自动恢复代理**，
  更新后无需手动重新启动代理

## v1.2.1

**弹窗反馈完善**

- 服务器分享的**导出 / 导入**补上成功与失败弹窗反馈（原来只写运行日志，用户看不到结果）
- 失败弹窗不再把完整的系统错误原文塞进弹窗（改为简短说明，完整原因保留在运行日志），
  避免长文本换行把弹窗内容撑偏
- 本地 / WebDAV 备份、还原的成功与失败弹窗文案精简，统一原生 NSAlert 并置于屏幕上部
  居中，符合系统对话框观感

**构建修复**

- 修复 `UNIVERSAL=0 ./build.sh` 本地重复构建时 Go 编译报 "already exists and is not an
  object file" 中断整次构建、导致 Swift 界面从不重新编译的伪修复问题（编译前先删旧
  内核二进制）

## v1.2.0

**系统代理接管提速与可靠性**

- 接管系统代理只作用于**当前默认路由所在的活动网络服务**（原来所有网卡都设一遍），
  接管耗时从约 6 秒降到约 1 秒；解析活动服务失败时自动回退到全部服务兜底
- **VPN 冲突保护**：若默认路由落在虚拟接口（`utun`/`tap`/`ppp`/`ipsec` 等，
  即有 VPN 在跑），主动跳过系统代理接管，绝不和 VPN 抢流量，并给出明确提示
- 菜单栏图标语义修正：**接管成功（enable 完成 + 验证通过）后才变蓝**，
  不再在接管一开始就变蓝；「还原」标记与图标状态分离
- 查询默认路由的 `route` 命令加 5 秒超时兜底，杜绝意外挂起

**服务器切换修复**

- 修复：代理运行中通过状态栏「选择服务器」/ 主窗口下拉切换服务器**无效果**
  （旧实现运行时直接忽略 + 菜单项置灰）。现在运行中切换会**自动重启代理
  换到新服务器生效**（日志提示「会短暂断开」），与分流模式切换行为一致

**检查更新 / 分流数据修复（GitHub 限流 403）**

- 修复：接管代理后「检查更新」报「网络或 GitHub 不可达」。根因是隧道出口为
  Cloudflare/共享 IP，被 GitHub API 按出口 IP 限流（HTTP 403 "API rate limit
  exceeded"），程序解析不到版本号误报断网
- 网络策略改为**隧道优先、失败降级直连**：检查新版本、分流数据版本、geoip/geosite、
  DMG 下载都先走系统代理（隧道，通常更快）；拿不到结果（含 403 限流、节点故障）
  时自动用直连重试一次。直连是家庭独立 IP，能避开共享机房 IP 的 API 限流；
  未开代理时本就直连，无需特判

## v1.1.14

**修复（幽灵弹窗）**

- 修复「检查更新 / 显示应用」后屏幕右上角菜单栏下方残留的**透明空窗**（幽灵弹窗）：accessory（无 Dock 图标）模式下 NSAlert 模态窗口结束后有时不会自动消失，一次弹窗残留一个、越积越多，且「显示应用」会把历史残留的隐藏空窗一并拉出来
- 修复：模态弹窗结束立即隐藏窗口；「显示应用」只拉出主窗口，不再遍历全部 `NSApp.windows`

## v1.1.13

**分流模式（App 级）**

- 分流模式从「每台服务器各自设置」改为 **App 全局参数**：无论选中哪台服务器都遵从一个分流模式（若要配合 v2rayN 使用：建议 v2rayN 负责分流、这里选「全局模式」做纯隧道出口）
- 切换分流模式**立即生效并自动保存**（与「自动设置系统代理」等全局开关一致）：运行中切换自动重启内核生效，不再要求点「保存」，重启后也不会回退
- 升级后旧版本各服务器的分流模式统一重置为默认「绕过中国大陆」

## v1.1.12

**服务端部署（重要）**

- ⚠️ **Worker 必须把「兼容日期」设为 `Sep 15, 2025`（或更新）**：否则隧道握手
  能通但数据不通，表现为「能连上但打不开任何网站」（CONNECTED 后连接挂起卡死，
  443 数据黑洞）。`wrangler.toml` 已带 `compatibility_date = "2025-09-15"`，
  命令行部署自动生效；网页 Dashboard 部署需手动设置
  （Settings → Running → Compatibility Date）

**自检误杀修复（多站点探针）**

- 国外站点探测不再只测 gstatic：依次探测 gstatic → cloudflare 两个
  **零字节 204 连通性端点**（专为测网络设计，返回 204 空页、无业务内容/CDN
  缓存干扰，比产品首页可靠），**任一通过**即判定隧道正常，自检里直接点名是哪个站点通的
- 修复：部分 CF 分片出口被 Google 系屏蔽（gstatic 超时）但 cloudflare 端点
  通畅时，探针不再误报「隧道失败」、系统代理也不会被误还原；全部失败时才
  报各站点原因，并自动重试（间隔 1s/3s）

**界面**

- 移除全部按钮 / 图标上的悬停提示气泡（`.help()`），界面更干净、无残留

**隧道兜底（防黑洞卡死）**

- 新增黑洞检测：客户端已向目标发送数据、服务端却 8 秒内从未返回任何数据时
  （典型是服务端「假成功」黑洞，如 Worker 兼容日期不对——握手通但出站数据
  根本不转发），自动断开并在日志里点名提示「可检查 Worker 兼容日期是否
  ≥ 2025-09-15」，不再让连接在转发循环里永久挂起
- 正常连接不受影响：服务端毫秒级就有回应（TLS 握手），一旦收到过数据即退出；
  客户端还没发数据的空闲连接也不误判（只看「发过数据却从未收到回应」）

**构建 / 发版（fork 自适应）**

- 更新源不再写死：`build.sh` 默认从 git remote 自动获取 owner/repo（环境变量
  `ECH_UPDATE_REPO` 可手动覆盖，CI 里由 `${{ github.repository }}` 传入），
  fork 后打包会自动指向 fork 自己的仓库，「检查更新」不受影响

## v1.1.11

在 v1.1.10 基础上改进：

**启动失败弹窗**

- 启动失败弹窗按 2+1 分类，一眼知道是哪一类问题，不再「请查看运行日志」一兜到底：
  - **TOKEN 与服务器端不一致**（认证失败 / 401）
  - **服务器连接失败**（域名解析失败、连接被拒、超时、握手失败、连接被重置）
  - 识别不出来才显示「请查看运行日志」
- 域名解析失败时进一步指认字段：填了优选IP/域名就是**「优选IP/域名」**错了，
  没填则是**「服务地址」**错了（按内核日志里的实际拨号目标区分）
- 弹窗标题统一改为「启动失败」（此前为「出错」），窗口关着也能一眼定位

**端口占用处理**

- 启动时发现监听端口被占用，不再只报一句「请关掉占用程序」——
  弹窗明确是谁占的（进程名 + PID），可一键**强制结束占用进程后自动重启**，
  也可取消保持停止
- 内核启动时绑定端口失败的兜底路径也走同一个确认弹窗；查不到占用者
  （如刚释放还在 TIME_WAIT）才提示稍后重试

**配置保存**

- 重构「已保存副本」模型：落盘只写点过「保存」的服务器，界面改了一半
  的参数绝不会被带进配置文件（未保存改动退出即丢弃）
- 有未保存改动时「保存」按钮**橙色高亮**提醒，鼠标悬停提示具体含义
- 启动前校验：配置被改过但没保存时先拦截，提示点「保存」再启动，
  不会带着改了一半的参数跑起来
- 修复：重命名服务器后退出会丢名字（此前只改内存、未落盘，且误报
  「有未保存改动」），现在重命名即时同步到磁盘

**服务端 TOKEN**

- `Worker-ECH.js` 的 TOKEN 校验改为**无论是否设置都校验**：服务端设了
  TOKEN 就要求客户端完全一致；服务端没设 TOKEN 时，客户端填了任何
  非空值也会被 401 拒绝，不会再出现「明明填了 TOKEN 却被当成公开代理」的歧义

## v1.1.10

> 注：v1.1.7 / v1.1.8 / v1.1.9 的改动此前未在 GitHub 单独发版，随
> v1.1.10 一起发布，下方为**自 v1.1.6 以来的全部更新**。

**启动体验**

- 修复：启动/停止代理不再卡成风火轮 —— 接管/还原系统代理原本要同步跑
  几十次 `networksetup` 子进程（3 个网络服务 × 每服务 4~8 次），主线程被
  卡住几秒。现在统一挪到后台 actor 串行执行，`stop()` 里等内核退出、
  端口释放的忙等也改成异步，点「启动/停止代理」即时响应
- 启动提速：不再等完整的 HTTP 连通性探针才接管系统代理 —— 内核
  是"隧道建好才监听端口"，现在端口一就绪就立刻接管系统代理
  （约 5 秒上限，对齐内核 10 秒握手窗口），探针改到后台跑、只更新
  自检状态，启动到"网页可访问"明显变快

**自检**

- 新增：自检按钮**任何状态都能点** —— 代理运行中跑完整连通性检测，
  未启动时跑预检（TCP 测服务器端口 + DoH 443 可达性），人工确认配置
  能用了再启动；启动代理时自动静默检测一次，运行中随时可手动复查
- 修复：连通性探针重试间隔拉开（600ms 一次 → 最多 3 次、间隔 1s/3s），
  远程出口（共享机房 IP）对个别站点（如百度）瞬时封禁几十秒后恢复的
  场景不再误报失败整灯变红
- 修复：自检按钮图标不再"没测过也绿" —— 刚启动、记录还是空的时
  图标也绿着，明摆着没跑却装成通过。现在和状态灯一致：待检测中性色、
  检测中置灰、通过才绿、失败才红
- 修复：手动点自检按钮不再把每项结果重复记录两遍
- 连通性探针超时从 15 秒缩短到 8 秒，自检更快出结果

**连通性与稳定性**

- 修复：ECH 公钥查询（DoH）在 IPv6 路由不通的网络里失败——
  `cloudflare-dns.com` 这类双栈 DoH 会先解析出 IPv6 地址，连接后
  读数据直接被 reset（connection reset by peer）。现在 DoH / UDP DNS
  拨号一律 **IPv4 优先**，IPv4 不可达时才退回 IPv6，国内网络更稳
  （若 DoH 服务器本身被墙，请使用内置的国内 DoH，如 `dns.alidns.com/dns-query`）
- 修复：隧道探针失败且系统代理已被接管时，自动把系统代理还原回去，
  避免浏览器全部走一个"死隧道"打不开网页（只依据"必须经隧道"的国外
  站点探针，国内直连/本地端口失败不触发）
- 修复：`start()` 极端路径（服务器被删、配置丢失）不再把状态假标成
  "运行中"，界面不会亮绿灯但内核其实没了

**系统代理**

- 系统代理备份/还原补上 PAC 自动代理：
  - 接管前备份各网络服务的 PAC 地址与开关，还原时原样恢复，
    不会再把 ShadowsocksX / ClashX / V2rayU 这类工具的 PAC 设置抹掉
  - 接管期间临时关闭 PAC，避免 PAC 和 EchOS 的 socks/http 抢浏览器流量
  - 还原逻辑保持"没有备份就绝不动系统设置"的安全原则不变

**内部**

- 窗口高度统一走 `minContentHeight` 常量，消除魔法数字

## v1.1.9

在 v1.1.8 基础上改进：

- 修复：启动/停止代理不再卡成风火轮 —— 接管/还原系统代理原本要同步跑
  几十次 `networksetup` 子进程（3 个网络服务 × 每服务 4~8 次），主线程被
  卡住几秒。现在统一挪到后台 actor 串行执行，`stop()` 里等内核退出、
  端口释放的忙等也改成异步，点「启动/停止代理」即时响应
- 修复：`start()` 极端路径（服务器被删、配置丢失）不再把状态假标成
  "运行中"，界面不会亮绿灯但内核其实没了
- 修复：隧道探针失败且系统代理已被接管时，自动把系统代理还原回去，
  避免浏览器全部走一个"死隧道"打不开网页（只依据"必须经隧道"的国外
  站点探针，国内直连/本地端口失败不触发）
- 修复：手动点自检按钮不再把每项结果重复记录两遍
- 内部：窗口高度统一走 `minContentHeight` 常量，消除魔法数字

## v1.1.8

在 v1.1.7 基础上改进：

- 修复：ECH 公钥查询（DoH）在 IPv6 路由不通的网络里失败——
  `cloudflare-dns.com` 这类双栈 DoH 会先解析出 IPv6 地址，连接后
  读数据直接被 reset（connection reset by peer）。现在 DoH / UDP DNS
  拨号一律 **IPv4 优先**，IPv4 不可达时才退回 IPv6，国内网络更稳
- （注意：若 DoH 服务器本身被墙，例如 `cloudflare-dns.com` 在国内
  无法直连，请使用内置的国内 DoH，如 `dns.alidns.com/dns-query`）

## v1.1.7

在 v1.1.6 基础上改进：

- 启动提速：不再等完整的 HTTP 连通性探针才接管系统代理 —— 内核
  是"隧道建好才监听端口"，现在端口一就绪就立刻接管系统代理
  （约 5 秒上限，对齐内核 10 秒握手窗口），探针改到后台跑、只更新
  自检状态，启动到"网页可访问"明显变快
- 连通性探针超时从 15 秒缩短到 8 秒，自检更快出结果
- 系统代理备份/还原补上 PAC 自动代理：
  - 接管前备份各网络服务的 PAC 地址与开关，还原时原样恢复，
    不会再把 ShadowsocksX / ClashX / V2rayU 这类工具的 PAC 设置抹掉
  - 接管期间临时关闭 PAC，避免 PAC 和 EchOS 的 socks/http 抢浏览器流量
  - 还原逻辑保持"没有备份就绝不动系统设置"的安全原则不变

## v1.0.0

首个正式版发布。

- ECH 加密隧道（Go 内核 + SwiftUI 界面，单 App 完成代理全部功能）
- 多服务器管理：新增即弹「新建服务器名称」，起名后才能使用
- 只有点过「保存」的服务器才会落盘，未保存的服务器退出后自动丢弃
- 有未保存的服务器时禁止新建下一个，避免误操作；服务器名称唯一，重名会被拦截
- 三种分流模式：绕过中国大陆 / 黑名单 / 全局，支持自定义分流规则（geosite / geoip）
- 系统代理一键接管，停止时自动还原
- SOCKS5 + HTTP 双协议本地代理
- 浅色 / 深色界面自动适配，跟随 macOS 系统外观
- 自动检查更新：启动时静默检查 GitHub Releases，发现新版提示一键下载 DMG；
  分流数据（geoip / geosite）随上游自动更新到本地数据目录
- 连通性自检、开机自启、菜单栏常驻、程序坞图标开关
- TOKEN 可选，与 Workers 服务端环境变量一致

## v1.0.1

在 v1.0.0 基础上改进：

- 检查更新改为独立弹窗：菜单栏触发时不再强制打开主窗口；「已是最新 / 检查失败」也走独立对话框
- 关闭窗口改为隐藏（不再销毁），避免 SwiftUI 在 App 激活时自动恢复主窗口
- 自动更新链路完善：确认更新后自动下载 DMG、替换 /Applications 里的旧版并重启，失败退回手动方式
- 服务器名称长度限制：最多 8 个汉字 / 16 个英文（显示宽度），输入时实时截断
- 服务器分享/导入数据安全：
  - 导入只接收参数完整的服务器（服务地址/端口等必需，token 可选）
  - 名称唯一：撞名时若服务地址+端口不同则自动改名「-01」防重复，完全相同则跳过
  - 导入后删除未保存的空壳/草稿服务器，不产生垃圾配置
- 备份只含已保存服务器；还原前逐台校验，参数不完整的自动跳过
- 若干底层修复：`config.save()` 改为 `persist()`，杜绝未保存草稿被连带写盘

## v1.1.6

在 v1.1.5 基础上改进：

- 修复：检查更新失败的弹窗文案过长导致换行，原生 NSAlert 退回左对齐
  （标题和正文都不居中）—— 文案缩短为单行，恢复居中显示
- 清理一处残留的死代码

## v1.1.5

在 v1.1.4 基础上改进：

- 检查更新弹窗整理为统一的 `runModalCentered`（原生 NSAlert）
- 弹窗内容（logo / 标题 / 正文 / 按钮）在 macOS 13+ 上居中显示；
  窗口水平居中、垂直位置遵循 macOS 原生行为
- 移除临时的调试代码

## v1.1.4

在 v1.1.3 基础上改进：

- 检查更新全流程弹窗内容统一居中：
  - 「已是最新 / 检查失败 / 发现新版本 / 更新完成」弹窗窗口在屏幕居中
  - 下载进度面板的文字、进度条、取消按钮全部水平居中
- 更新完成弹窗删除多余说明文字，只保留「更新完成 + 已升级到 vX」

## v1.1.3

在 v1.1.2 基础上改进：

- 检查更新的弹窗统一改用原生 NSAlert（macOS 13+ 图标与文字居中显示）
  - 「发现新版本」：加粗标题显示当前版本，正文显示新版本
  - 「已是最新 / 检查失败 / 更新完成」均为原生居中弹窗
- 移除多余的自定义弹窗代码

## v1.1.2

在 v1.1.1 基础上改进：

- 修复：代理运行中分享/备份被禁用 —— 分享导出/导入、本地与 WebDAV 备份/还原
  只读写服务器数据，不影响运行中的代理，现在运行中也可以正常操作
- （还原仍会先停止代理并有提示，行为不变）

## v1.1.1

在 v1.1.0 基础上改进：

- 下载更新时弹出独立进度面板（带进度条），不再"无声下载"
- 下载过程可取消：面板「取消」按钮（或 Esc），中断后不误报失败
- 更新完成后不再弹出主窗口：自动替换重启后只弹独立「更新完成」对话框，保持菜单栏常驻

## v1.1.0

在 v1.0.1 基础上（代码树已含，待正式发版）：

- 服务器分享：勾选要分享的服务器 → 导出成文件（.json）给其他设备，或从分享文件一键导入
- 整配置备份：本地文件 / WebDAV 两种方式，可随时还原
  - WebDAV 密码存系统钥匙串，config.json 里只有地址和用户名
  - WebDAV 备份目录可选，留空默认 `EchOS_Backup`
- 服务器名称长度限制：最多 8 个汉字或 16 个英文/符号，选择器不再显示「…」
